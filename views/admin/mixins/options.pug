
mixin options(categoryList, i)
    each item in categoryList 
        option(value=item.id) #{"- - ".repeat(i) + item.name}            
        if (item.children && item.children.length > 0)
            +options(item.children, (i + 1))
        